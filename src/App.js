import React, {Component} from 'react';

class App extends Component {
  state = {  }
  render() { 
    return ( 

      // TODO: return each component
      // ? Rendered by a <Router /> component?
      // ? How will you create a menu for this?
      // ! Check http-league for the bootstrap template

     );
  }
}
 
export default App;
