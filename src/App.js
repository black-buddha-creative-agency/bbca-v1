import React, { Component } from 'react'
import Layout from './components/Layout/index'

class App extends Component {
  state = {}
  render() {
    return (
      <Layout>
        <div>
          Components{' '}
          {/* 
        
        
      // TODO: return each component as a style guide or just each individual component sized by a div
     // ? Rendered by a <Router /> component?
     // ? How will you create a menu for this?
     // ! Check http-league for the bootstrap template
     
     */}
        </div>
      </Layout>
    )
  }
}

export default App
